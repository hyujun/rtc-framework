// Scaffolding smoke test: verifies package compiles, links, and tests run.
// Real unit tests arrive in Steps 2-9.

#include <gtest/gtest.h>

TEST(Scaffold, CompilesAndLinks) {
  EXPECT_TRUE(true);
}
