# rtc_tools/requirements.txt 의 상위 집합 (Option A)
# PyTorch 는 현재 워크스페이스 미사용 — 사용 시점에 재추가.
numpy>=1.24.3
scipy>=1.10.1
matplotlib>=3.5.3
pandas>=1.5.3
mujoco>=3.0.0
Cython
# colcon-core 0.20.1 호환
setuptools<80
# PyQt5, rclpy, ament_* 는 system-site-packages 에서 로드
